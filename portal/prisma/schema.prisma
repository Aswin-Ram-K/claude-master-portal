generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model SessionLog {
  id              String   @id
  sessionId       String
  repoOwner       String
  repoName        String
  branch          String?
  startedAt       DateTime
  endedAt         DateTime?
  durationSeconds Int?
  summary         String?
  filesChanged    String   @default("[]")
  commits         String   @default("[]")
  inputTokens     Int?
  outputTokens    Int?
  model           String?
  entrypoint      String?
  toolsUsed       String   @default("[]")
  subagents       String   @default("[]")
  userMessages    String   @default("[]")
  rawLog          String   @default("{}")
  syncedAt        DateTime @default(now())

  @@index([repoOwner, repoName])
  @@index([startedAt])
  @@map("session_logs")
}

model Repo {
  id            String    @id
  owner         String
  name          String
  lastSessionAt DateTime?
  totalSessions Int       @default(0)
  htmlUrl       String?

  @@map("repos")
}

model ChatMessage {
  id             Int      @id @default(autoincrement())
  conversationId String
  role           String
  content        String
  metadata       String   @default("{}")
  cliSessionId   String?
  createdAt      DateTime @default(now())

  @@index([conversationId, createdAt])
  @@map("chat_messages")
}
